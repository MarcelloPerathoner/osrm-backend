# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.13"
  jobs:
    install:
      - bash scripts/install_venv.sh
    build:
      html:
        - BUILDDIR=$READTHEDOCS_OUTPUT make -C docs
